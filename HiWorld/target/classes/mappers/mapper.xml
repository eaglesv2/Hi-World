<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hiworld.user.service.dao.UserDAO">
	<select id="getAllSawon" resultType="sawonVO">
	SELECT id, name, tel
	FROM teltable55
	</select>
	<select id="getSawon" resultType="sawonVO">
	SELECT id, name, tel
	FROM teltable55
	WHERE id=#{id}
	</select>
	<insert id="insertSawon">
	INSERT INTO HW_USERLIST(UserName, UserID, UserPW, UserGender, UserBirth, UserTel, UserAddress)
	VALUES (#{userName},#{userId},#{userPw},#{userGender},#{userBirth},#{userTel},#{userAddress})
	</insert>
	<update id="updateSawon">
	UPDATE teltable55
	SET id=#{id}, tel=#{tel}
	</update>
	<delete id="deleteSawon">
	DELETE FROM teltable55
	WHERE name=#{name}
	</delete>
</mapper>